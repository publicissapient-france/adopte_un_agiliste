<div class="agiliste">
    <spanclass="agiliste-miniature">
        <img src="{{image_url}}" alt="{{name}}"
                />

        <div class="mask">
            <div class="adopt-me-marker"></div>
        </div>
        <div class="adopt-me">
            <span class="link-to-adopt">Adoptez moi&nbsp;!</span>
        </div>

    </span>

    <div class="title">{{title}}</div>
    <div class="description">{{description}}</div>
</div>